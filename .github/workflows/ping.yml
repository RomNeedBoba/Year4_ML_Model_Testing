name: Keep Render Awake

on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app
        run: curl -s https://year4-ml-model-testing.onrender.com/detect> /dev/null
